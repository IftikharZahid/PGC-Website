{
  "name": "pgc-website",
  "version": "1.0.0",
  "description": "College Management Website - Punjab Group of Colleges",
  "type": "module",
  "scripts": {
    "start": "node server/server.js",
    "server": "cd server && npm install && npm start",
    "client": "cd client && npm install && npm run dev",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "build": "cd client && npm install && npm run build",
    "install-all": "npm install && cd server && npm install && cd ../client && npm install"
  },
  "keywords": [
    "college",
    "management",
    "education",
    "pgc"
  ],
  "author": "Iftikhar Zahid",
  "license": "ISC",
  "dependencies": {
    "mongodb": "^7.0.0"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  },
  "engines": {
    "node": ">=18.0.0",
    "npm": ">=9.0.0"
  }
}